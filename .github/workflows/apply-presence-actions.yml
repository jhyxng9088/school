name: Apply presence and reminder actions

on:
  push:
    paths:
      - 'scripts/apply-presence-actions.mjs'
      - 'scripts/apply-presence-actions-trigger.txt'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - run: node scripts/apply-presence-actions.mjs
      - run: npm install
      - run: npm run build
      - run: npx --yes firebase-tools@latest emulators:exec --only firestore --project demo-school-sync "echo rules-ok"
      - name: Commit verified migration
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/school-sync.js src/main.jsx src/styles.css src/todo-stage5-ai.jsx src/todo-stage5.css firestore.rules public/sw.js
          git commit -m "Add class presence and explicit reminder actions"
          git push
