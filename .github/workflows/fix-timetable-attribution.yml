name: Fix timetable attribution

on:
  push:
    branches: [main]
    paths:
      - 'scripts/fix-timetable-attribution.mjs'
      - '.github/workflows/fix-timetable-attribution.yml'

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply guarded timetable patch
        run: node scripts/fix-timetable-attribution.mjs
      - name: Install dependencies
        run: npm install --package-lock=false --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Remove temporary validation files
        run: |
          rm -f scripts/fix-timetable-attribution.mjs
          rm -f .github/workflows/fix-timetable-attribution.yml
      - name: Commit validated changes
        run: |
          git config user.name 'school-pwa-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git commit -m 'Fix timetable editing and attribution' || exit 0
          git push
